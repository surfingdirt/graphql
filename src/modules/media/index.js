const MediaAPI = require('./media.api');
const Media = require('./media.type');
const MediaQueryResolvers = require('./media.resolvers');

module.exports = {
  Media,
  MediaAPI,
  MediaQueryResolvers,
};
