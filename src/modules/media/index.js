const Media = require("./media.type");
const {
  MediaTypeResolvers,
  MediaFieldResolvers
} = require("./media.resolvers");

module.exports = {
  Media,
  MediaTypeResolvers,
  MediaFieldResolvers
};
