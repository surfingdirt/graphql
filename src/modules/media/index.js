const Media = require("./media.type");
const MediaAPI = require("./media.api");
const {
  MediaQueryResolvers,
  MediaTypeResolvers,
  MediaFieldResolvers
} = require("./media.resolvers");

module.exports = {
  Media,
  MediaAPI,
  MediaQueryResolvers,
  MediaTypeResolvers,
  MediaFieldResolvers
};
