const Media = require("./media.type");
const MediaAPI = require("./media.api");
const {
  MediaTypeResolvers,
  MediaFieldResolvers
} = require("./media.resolvers");

module.exports = {
  Media,
  MediaAPI,
  MediaTypeResolvers,
  MediaFieldResolvers
};
