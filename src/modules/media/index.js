const Media = require("./media.type");
const MediaAPI = require("./media.api");
const getMediaResolvers = require("./media.resolvers");

module.exports = {
  Media,
  MediaAPI,
  getMediaResolvers,
};
