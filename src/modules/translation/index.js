const Translation = require("./translation.type");
const TranslationAPI = require("./translation.api");
const getTranslationResolvers = require("./translation.resolvers");

module.exports = {
  Translation,
  TranslationAPI,
  getTranslationResolvers,
};
