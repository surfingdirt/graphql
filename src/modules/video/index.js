const Video = require("./video.type");
const {
  VideoMutationResolvers,
  VideoQueryResolvers,
} = require("./video.resolvers");

module.exports = {
  Video,
  VideoMutationResolvers,
  VideoQueryResolvers,
};
